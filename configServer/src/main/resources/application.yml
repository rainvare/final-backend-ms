server:
  port: ${PORT:8888}

spring:
  application:
    name: configServer

  cloud:
    config:
      server:
        git:
          uri: https://github.com/rainvare/config-final-backend-ms

